Nat : Type
Z : Nat
S : Nat -> Nat

Vec : Nat -> Type
Nil : Vec Z
Cons : Nat -> Vec n -> Vec (S n)

with : Nat -> Vec m -> Vec (S m)
with = Cons

succ : Nat -> Nat
succ = \n. S n

succ2 : Nat -> Nat
succ2 = \m. S m

